class <%= name %>::package::remove {
  $version = "<%= version %>"
  $iteration = "<%= iteration %>"

  file {
<% paths.each do |path| -%>
    "<%= fixpath(path) %>":
      <%= settings %>;
<% end # paths.each -%>
  }
}
